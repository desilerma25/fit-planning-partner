<h1> Welcome <%= current_user.name.titlecase %>! </h1> 

<% if current_user.workouts.empty? %>
    <h3> You have not a created a workout yet </h3>
<% else %>
    <h3> Here are your created workouts: </h3>
    <% current_user.workouts.each do |uw| %>
    <%= link_to "#{uw.name}", workout_path(uw) %>
    <% end %>
<% end %>

<br>
<%= link_to "Need insome inspiration?", workouts_path %>