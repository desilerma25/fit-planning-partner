<h1> Welcome <%= @user.name.titlecase %>! </h1> 

<% if @user.workouts.empty? %>
    <h3> You have not a created a workout yet </h3>
<% else %>
    <h3> Here are your created workouts: </h3>
    <% @user.workouts.each do |uw| %>
    <%= link_to "#{uw.name}", workout_path(uw) %>
    <% end %>
<% end %>

<br>
<%= link_to "Need some inspiration?", workouts_path %>